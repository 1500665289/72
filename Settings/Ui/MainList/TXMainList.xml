<?xml version="1.0" encoding="utf-8"?>
<MainList>
  <List>
    <Data Kind="Builds" Unlock="0" Type="工作" CMDType="Build" Name="TXHandWorkTable" />
    <!-- 手工桌 -->
    <Data Kind="Builds" Unlock="0" Type="工作" CMDType="Build" Name="TXStoneMaker" />
    <!-- 切石桌 -->
    <Data Kind="Builds" Unlock="0" Type="工作" CMDType="Build" Name="TXWeaponMaker" />
    <!-- 锻铁台 -->
    <Data Kind="Builds" Unlock="0" Type="工作" CMDType="Build" Name="TXFurnace" />
    <!-- 熔炉 -->
    <Data Kind="Builds" Unlock="0" Type="工作" CMDType="Build" Name="TXTailorTable" />
    <!-- 缝纫台 -->
    <Data Kind="Builds" Unlock="0" Type="工作" CMDType="Build" Name="TXTextileTable" />
    <!-- 纺织机 -->
    <Data Kind="Builds" Unlock="0" Type="门派" CMDType="Build" Name="Building_TXMagicCushion" />
    <!-- 法座 -->
  </List>
</MainList>
